[Unit]
Description=Sieve — hourly pull and recompute

[Timer]
OnCalendar=hourly
# Artificial Analysis allows 1 000 requests a day; spreading the start keeps a
# fleet of these off the same second.
RandomizedDelaySec=180
Persistent=true
Unit=sieve-pull.service

[Install]
WantedBy=timers.target
