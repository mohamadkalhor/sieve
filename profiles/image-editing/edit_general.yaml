name: edit_general
modality: image-editing
purpose: "editing an existing image to instruction"
weights:
  quality: 0.8
  maturity: 0.2
require:
  min_appearances: 500
shape: {images: 1}
policy:
  margin: 15.0
  max_tenure_days: 30
  chain: 3
  min_confidence: 0.5
