# `anime` is the published category closest to drawn style; it is broader
# than the name suggests.

name: image_illustration
modality: text-to-image
purpose: "drawn and stylised work"
weights:
  quality: 0.4
  anime: 0.4
  maturity: 0.2
require:
  min_appearances: 500
shape: {images: 1}
policy:
  margin: 15.0
  max_tenure_days: 30
  chain: 3
  min_confidence: 0.5
